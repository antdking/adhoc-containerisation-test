
upstream app {
    server ${APP_HOST}:${APP_PORT};
}

server {
    listen 80;
    return 301 https://$host$request_uri;
}

server {
    listen 443;

    location / {
        proxy_pass http://app;
    }
}
